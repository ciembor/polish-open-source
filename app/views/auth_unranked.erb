<section class="hero">
  <div class="hero__inner">
    <p class="eyebrow"><%= h t('auth.unranked.eyebrow') %></p>
    <h1><%= h t('auth.unranked.title') %></h1>
    <p class="lede">
      <%= h t('auth.unranked.body', login: session[:unranked_github_login] || 'GitHub') %>
    </p>
    <a class="ranking-action" href="<%= h app_path('/latest') %>"><%= h t('rankings.back') %></a>
  </div>
</section>
