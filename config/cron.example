# Run shortly after the previous month has closed.
# Update the path to match the deployment directory.
0 0 1 * * cd /srv/polish-open-source-rank && bundle exec ruby bin/monthly_rankings >> log/monthly_rankings.log 2>&1
